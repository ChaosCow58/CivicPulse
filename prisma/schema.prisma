generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mongodb"
}

model users {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
